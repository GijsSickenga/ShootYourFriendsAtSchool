using UnityEngine;
using System.Collections;

public class ColorTracker : MonoBehaviour {
    public static Color[] colors = { Color.magenta, Color.green, Color.red, Color.blue };
    public static bool[] playerActive = new bool[4];
}
